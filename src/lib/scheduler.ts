// Scheduling is handled by Cloudflare Cron Triggers (see cron-worker/).
// The cron worker makes an authenticated POST to /api/sync every 10 minutes.
// This file is retained for reference only and is not imported anywhere.
export {};
