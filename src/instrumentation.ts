// DB migrations are applied via: wrangler d1 migrations apply cf_spend --remote
// Scheduled syncs are handled by a Cloudflare Cron Trigger Worker (see cron-worker/)
export async function register() {}
